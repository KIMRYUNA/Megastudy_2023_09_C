// * 클래스: 객체(템플릿) 지향 프로그래밍에서
// 데이터(struct)와 이 데이터를 처리하는 함수(function)를 하나로 묶은
// 사용자 정의 데이터 타입(class)

//*객체는 클래스의 인스턴스를 의미.


//*클래스 : 객체의 설계도, 템플릿
//*객체는 클래스에 정의된 대로 생성된 실체(entity)입니다.
//*클래스 내에서 데이터는 멤버 변수(struct), 함수는 멤버 함수(function)으로 정의.
